import React from 'react';
import { Typography, Box } from '@material-ui/core';
import { Title } from '../title/title';

export const About = () => {

  return (
    <Box>
      <Title/>
    </Box>
  );
}