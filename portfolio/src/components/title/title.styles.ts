import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  title: {
    textAlign: 'center',
    fontFamily: 'system-ui',
  },
  subtitle :{
    fontFamily: 'system-ui',
    fontWeight: 'lighter',
    textAlign: 'center',
  }
});
